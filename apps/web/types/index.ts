export enum subscription_status {
  on_trial = 'on_trial',
  active = 'active',
  paused = 'paused',
  past_due = 'past_due',
  unpaid = 'unpaid',
  cancelled = 'cancelled',
  expired = 'expired',
}

export enum account_type {
  REGULAR = 'REGULAR',
  SAVINGS = 'SAVINGS',
  DEBT = 'DEBT',
}
